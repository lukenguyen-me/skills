# 01: Record the parent feature branch name

**What to build:** After discovery, the run can name the parent integration branch from the parent ticket id.

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] The parent branch template includes the parent ticket id.
- [ ] The branch name is not `main`, `master`, or `develop`.
